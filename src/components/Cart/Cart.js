import React from 'react';
import type {Node} from 'react';
import {
  ScrollView,
  StatusBar,
  Text,
  TextInput,
  useColorScheme,
  View,
  FlatList,
  Image,
} from 'react-native';

import style from './Cart.styles';

const Cart: () => Node = ({data}) => {
  return <Text>Deneme</Text>;
};

export default Cart;
